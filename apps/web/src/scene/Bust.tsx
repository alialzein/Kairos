"use client";

/** Phases 2 + 3 — bust geometry and the contour-line material (stub: renders nothing yet).
 *  `contours` false keeps the Phase 2 flat fill so the silhouette can be judged alone. */
export function Bust({ contours }: { contours: boolean }) {
  void contours;
  return null;
}
