{
  "name": "@twin/svc-brain",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "lint": "uv run ruff check . && uv run ruff format --check .",
    "typecheck": "uv run mypy",
    "test": "uv run pytest -q"
  }
}
