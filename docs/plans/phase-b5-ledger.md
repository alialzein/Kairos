# Phase B5 SDD ledger (copy)

Verbatim copy of the git-ignored .superpowers/sdd/phase-b5/progress.md taken at the 2026-09-06 hand-off from the laptop to the gaming PC. To resume: copy this file back to .superpowers/sdd/phase-b5/progress.md (create the directory) and run superpowers:subagent-driven-development on docs/plans/phase-b5.md; it resumes at the first task without a complete line (Task 6).

---

# SDD ledger — plan: docs/plans/phase-b5.md

Spec: docs/06-avatar-spec.md (+ docs/11 B5 table, packages/shared contracts, Ali's 2026-09-04 answers). Plan committed on main at d2e4675. Execution started 2026-09-04.

## Pre-flight scan

| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1 → T3 | `ROLE_SPLIT`, `TIERS` from @twin/config | names match |
| T1 → T5 | `TIERS`, `SHAPE_ID` (HUMANOID 0, ORB 1, NEBULA 2, RING 3), `AVATAR_STATES`, `Easing`; `tier.ts` (`baseTier/stepDown/tierFromProbe/parseTierOverride/readSignals`); `FrameStats` | match; `SHAPE_ID` order matches `shapeAt()` select chain |
| T2 ↔ T3 | `boundsOf` exists in both `scripts/lib/obj.ts` and `apps/web/src/avatar/sim/sampler.ts` | duplication across two packages that cannot import each other (root scripts vs web app) — Ruling: accepted, 15 lines — cost if wrong: a shared helper package later |
| T3 → T5 | `Targets {n, coreEnd, spineEnd, humanoid, orb, nebula, ring, regions, spineT, waves}`, `strided`, `loadBust` | match |
| T4 → T5 | store `since` (ms), `tuning`, `pointer.strength` ±1, `Energy` from energy.ts | match; T5 divides `since` by 1000 |
| T4 → T8 | `AvatarEvent`, `useAvatarState().send`, `setEnergy`, `playWakeCue` | match |
| T5 → T6 | `AvatarCanvas` gains `PostPass` + aberration ref; `frame.ts` already emits `aberration` | consistent |
| T5 → T7 | `DEFAULTS` exported from `sim/frame.ts`; `Tuning` keys = Leva controls | all eleven keys present in `Tuning` |
| T5 → T8 | `BenchAvatar` demo branch renders `AvatarStage`; `onWake`/`onReady` props | consistent |
| T5 → T9 | `window.__twinAvatar.stats {p50,p95,count}` | consistent across BenchAvatar and both specs |
| T1 self | test asserts OFFLINE morphDuration ≤ 2.0, others ≤ 1.2 | table complies |
| T2 self | OBJ fixture: `body` faces reference v1–v8 only; helper face excluded | consistent; `fetch` + top-level await under root `"type": "module"` |
| T3 self | `regionsFor` neck point y=0.1 → yN 0.56 ∈ (0.5, 0.62] | consistent (fixed pre-execution) |
| T4 self | `binRange` test bins 2..4 inside bass [1..6], outside mid [5..43] | consistent (fixed pre-execution) |
| T5 self | `frame.test` expectations vs table (IDLE pulse 0.45–0.75, SPEAKING gain 0.5, LISTENING gain 0.4) | consistent |
| T8 self | ribbon-updater lambda appears twice in `AvatarStage` (submit + onReady) | rubric: verbatim duplication — Ruling: implementer factors one `pushRibbon` callback; carried in the T8 dispatch — cost if wrong: none |
| T9 self | `UPDATE_BASELINE` expression on push events → '0' | consistent |

Pre-flight rulings:
- Ruling: no git worktree; task branches `b5-NN-*` are created in the main checkout from `main`, PR per task, controller squash-merges after CI is green (plan D1, approved by Ali 2026-09-04) — why: branch-per-task + PR is the repo convention and gives isolation without a second checkout — cost if wrong: a stray uncommitted file could ride along; `git status` is checked before each dispatch.
- Ruling: pushes, PR creation and squash-merges are durably authorised by Ali's approval of plan D1 ("Approve and execute") — cost if wrong: a merge Ali would have wanted to review first; every PR stays visible on GitHub.
- Ruling: local Python service checks stay out of the B5 gate (uv launchers are quarantined on this laptop; CI runs them) — cost if wrong: none for B5 (no Python changes).

## Task log

Task 1: implementer acaa291de97024f8c (sonnet), BASE d2e4675, branch b5-01-foundations, commit 2c8cebe.
Task 1: review (sonnet) spec ✅, quality Approved. ⚠️ resolved by controller: WAKING ORB→RING→HUMANOID sequencing lives in Task 5 `frame.ts` (plan); IDLE breathing period 4 s is hard-coded in the Task 5 kernel (`sin(time·π/2)`); `readSignals` is browser-only and exercised by the bench page (Task 5) — no gap.
Task 1: minor (deferred): report narrative miscounted pre-existing config tests (3, not 4) — no code impact.
Task 1: minor (deferred): `TIERS.low.targetFps = 0` comment says "reduced motion" but Low is also reachable from weak-hardware signals; clarify the comment when the renderer consumes `targetFps` (Task 5).
Task 1: complete (commits d2e4675..2c8cebe, review clean). PR #2 opened by controller; squash-merge on green CI.

Task 2: implementer ab05ee6dccd2829a5 (sonnet), BASE d2e4675, branch b5-02-bust, commit 66d25e8 — DONE_WITH_CONCERNS: (1) gltf-transform enum typing needed `?? fallback` + `Float32Array<ArrayBuffer>` casts; (2) Y-only crop keeps the upper arms of MakeHuman's A-pose figure (x ±1.19 normalised, 9 948 triangles).
Task 2: Ruling: add a width crop `X_CUT = 2.2` (MakeHuman decimetres; every vertex of a kept face must satisfy |x| ≤ 2.2) as `cropByAbsX(mesh, xMax)` in `scripts/lib/obj.ts` with a test, applied after `cropByY`; manifest records `xCut`; brief's z sanity figure (±0.35) was a guess — measured ±0.5 is real chest depth and stands — why: docs/06 §2 asks for a head + shoulders bust, not arms; the plan's Y_CUT-only recipe was a plan defect — cost if wrong: a re-run of `pnpm build:bust` and a new GLB (one commit).
Task 2: fix applied by the implementer before review (commit ecec1e9): `cropByAbsX`, X_CUT 2.2, manifest `xCut`; bust now 4 934 vertices / 9 736 triangles, x ±0.969, z ±0.504.
Task 2: review (sonnet, a090d37e1d434723e) spec ✅, quality Approved; Important (plan-mandated): no guard against an empty crop → `normalizeBust` would emit NaN silently. Ruling: add `if (cropped.faces.length === 0) throw new Error(...)` in `build-bust.ts` after the crop pipeline — the plan's recipe omitted it; a loud failure is the spec's "deterministic, reproducible" asset in practice — cost if wrong: none (one line).
Task 2: minor (deferred): `cropByAbsX` test uses contiguous indices, so it does not prove re-indexing (the `cropByY` test does).
Task 2: minor (deferred): `cropByAbsX` was written green-first (disclosed in the fix report); logic is trivial and tested.
Task 2: fix round 1/5 (1 addressed, 0 open — empty-crop guard in build-bust.ts + normalizeBust throw + test; commits ecec1e9..8aba8a8); re-review (haiku, a363d4220bbe9dbb6) clean. ⚠️ trailers verified by controller on both commits.
Task 2: complete (commits d2e4675..8aba8a8, review clean). PR #3 opened by controller; CI green.
Task 2: incident — `gh pr merge 3 --squash` failed with merge conflicts (branch cut from d2e4675, before Task 1's lockfile change) and the controller's script deleted the remote branch anyway, which closed PR #3. Recovery: rebase `b5-02-bust` onto `main` in a temporary worktree, take main's `pnpm-lock.yaml` and re-run `pnpm install --lockfile-only`, force-push, reopen PR #3 (or open a new one), merge on green. Ruling: from now on every task branch is cut from the freshly fetched `main`, and the merge script deletes the remote branch only after `gh pr view` reports MERGED — cost if wrong: another rebase.
Task 2: recovered — rebased onto main (5e3068f, 1fb26cc, 2bc4472), PR #3 could not be reopened (GitHub refuses after branch deletion), PR #4 opened instead, CI green, squash-merged as 7daf023 on main.

Task 3: implementer ac73f27ca5c01a002 (sonnet), BASE bdc86d3 (main after Task 1), branch b5-03-targets, commit 5abcc6e — DONE_WITH_CONCERNS: (1) the brief's noise test sampled (0.5,0.5,0.5), a simplex lattice vertex where every seed returns 0 — implementer added a coordinate offset inside `makeNoise`; (2) raw `curl()` magnitude (~3.8 avg) broke the nebula `rmax ≤ 2.2` test — implementer normalises the curl direction before scaling by 0.35.
Task 3: Ruling: (1) revert the offset — keep standard simplex, fix the test to sample (0.37, 0.61, 0.83) and regenerate the snapshots once — why: production code must not carry test-shaped hacks; the brief's test was the defect — cost if wrong: none (snapshots regenerate). (2) accept the normalised curl direction — why: bounded displacement is what the spec's "curl-noise scattered volume" needs and the bound is provable — cost if wrong: a slightly more uniform nebula than raw curl would give.
Task 3: rulings applied by the implementer before review (commit ece2db2; snapshots regenerated once).
Task 3: review (sonnet, a1bf4c73fcbcf0b34) spec ✅, quality Approved. ⚠️ resolved by controller: trailers verified on 5abcc6e and ece2db2; `sim/bust.ts` is browser-only and gets exercised by the Task 5 bench page.
Task 3: minor (deferred): `pick()` in `targets/index.ts` has no guard for `count > avail` (upscaling); `strided` only ever downsamples — add a guard/test when Task 5 wires the probe step-down.
Task 3: minor (deferred): `strided` alignment of regions/spineT is asserted by length only (brief's test design).
Task 3: minor (deferred): docs/06 says SPINE runs neck→chest; the brief's `ANCHORS` has no neck anchor, so the spine interpolates head→chest — cosmetic; revisit in the Task 7 playground if the tendrils look wrong.
Task 3: complete (commits bdc86d3..ece2db2, review clean). PR #5 squash-merged as b959698 on main.

Task 4: implementer a3de92add02ac27b3 (sonnet), BASE b959698 (main with Tasks 1–3), branch b5-04-state-audio, commit f907ba1 — DONE.
Task 4: review (sonnet, a6db1b32dcdcb7277) spec ✅, quality Approved; Important (plan-mandated): `pendingThink` in `useAvatarState` is only cleared when WAKE_DONE fires, so a WAKING interrupted by FAILURE/forced state leaves the flag set and a later plain wake auto-dispatches THINK. Ruling: fix — the effect clears `pendingThink` whenever the state is neither DORMANT nor WAKING (the queued THINK is consumed synchronously inside the WAKE_DONE timer before React re-renders, so the legitimate path still works); no unit test (browser-only hook, Vitest is node-only by D12) — the bench E2E in Task 8 exercises the queue — cost if wrong: a wrong THINKING after an interrupted wake, visible in the playground.
Task 4: minor (deferred): `now()` falls back to `Date.now()` when `performance` is undefined (SSR module eval) — harmless.
Task 4: minor (deferred): `transition("LISTENING","INACTIVITY")` has no test.
Task 4: minor (deferred): analyser smoothing (0.6) stacked with `smoothEnergy` may over-soften transients — tune in the Task 7 playground.
Task 4: fix round 1/5 (1 addressed, 0 open — pendingThink cleared outside DORMANT/WAKING; commits f907ba1..c614acc); re-review (haiku, a798a00536c663d50) clean; trailer verified on c614acc.
Task 4: complete (commits b959698..c614acc, review clean). PR opened by controller; squash-merge on green CI.
Task 5: Ruling: the Task 5 branch is cut from `b5-04-state-audio` (not `main`) so the implementer can start while PR #6 runs CI; before Task 5's PR the controller runs `git rebase --onto main b5-04-state-audio b5-05-sim` — why: Task 5 imports the store/energy modules that only exist on that branch until the squash lands — cost if wrong: one rebase with no expected conflicts (disjoint files).
Task 5: Ruling: `apps/web/src/proxy.ts` matcher additionally excludes `bench/` and `.glb` requests, because `updateSession` calls `supabaseEnv()` before checking public paths and would throw on `/bench/avatar` and on `/avatar/bust.glb` when `NEXT_PUBLIC_SUPABASE_*` are unset (CI's avatar job, and any laptop without `.env.local`); `/bench` stays in `PUBLIC_PREFIXES` as defence in depth — why: the bench page must run with zero Supabase configuration (plan D6) — cost if wrong: one regex line.
Task 4: PR #6 squash-merged as 8a9737f on main.
Task 5: implementer a9b8c15e7ee51e453 (sonnet), BASE c614acc (b5-04-state-audio), commit 2850ad8, rebased onto main as 5c7fafc on `b5-05-sim`, pushed as DRAFT PR #7 (Ali asked what to push while setting up the PC) — DONE_WITH_CONCERNS: (1) WebGL2 fallback never runs the particle kernel ("transformFeedbackVaryings: too many varyings": 8 storage buffers in the compute node); (2) NEBULA (DORMANT/OFFLINE) faint/sparse at camera z 3.1 fov 34 (half-height 0.95; nebula radius ≤ 1.95); (3) `packages/config` barrel no longer re-exports the Node-only identity loader (pulled `node:fs` into the client bundle); subpath `@twin/config/identity` added.
Task 5: Ruling (1): pack the four shape targets into ONE `instancedArray(4·n, "vec4")` (xyz + region in w, HUMANOID block first) and index it as `element(instanceIndex + shapeId·n)`, so the update kernel touches three storage buffers (positions, velocities, targets); `regions`/`spineT` stay as material-only attributes — why: docs/06 §1 requires the automatic WebGL fallback, and WebGL2 transform feedback guarantees only 4 varyings — cost if wrong: a kernel rewrite (~40 lines), snapshots untouched.
Task 5: Ruling (2): camera `position.z` 3.1 → 3.6 (half-height ≈ 1.10, so the ORB r = 1 and the bust y ± 0.9 fit with margin), nebula `randomInSphere(rng, 1.25)` with curl 0.3 (regenerate its snapshot once), DORMANT `brightness` 0.35 → 0.5 — why: the plan's numbers clipped the ORB and pushed the nebula outside the frustum; Ali tunes the final look in Task 7 — cost if wrong: three numbers.
Task 5: Ruling (3): accepted — `identityPath`/`loadIdentity`/`Identity` move to the `@twin/config/identity` subpath; the barrel stays browser-safe — why: a client bundle cannot carry `node:fs`; no consumer used the barrel path — cost if wrong: an import path.
Task 5: rulings applied by the implementer before review (commit d5c3fff): packed `tAll` vec4 buffer (kernel = positions, velocities, tAll), camera z 3.6, nebula r 1.25 / curl 0.3 (snapshot regenerated), DORMANT brightness 0.5; root scripts typecheck clean. Residual: the "too many varyings" error is gone, but three 0.185.1's WebGL2 NodeBuilder intermittently fails to compile the sprite vertex shader (`nodeAttribute8` undeclared, ~50 % of fresh runs) and once read the wrong shape block; WebGPU path correct.
Task 5: Ruling: the WebGL2 residual is PARKED as a follow-up owned by the gaming-PC session (Ali's instruction 2026-09-05): branch `b5-05-webgl-fallback` after PR #7 merges; candidate fixes sent to that session (drop float storage attributes from the material, slot-buffer targets, or a three version bump) — why: WebGPU is the primary path on Chrome desktop/Android and Safari 26+; the task's spec-visible deliverable (particles render, tiers, bench, smoke) is met — cost if wrong: browsers without WebGPU show waves only until the follow-up lands; the B5 gate's phone check must note which backend ran.
Task 5: gaming-PC evidence (RTX 5070, Chrome, WebGPU) on d5c3fff: ultra IDLE p50 5.0 ms / p95 5.1 ms; high IDLE 5.0 / 5.1; SPEAKING 5.0 / 5.1; DORMANT 5.0 / 5.2 (vsync-bound) — desktop 60 fps gate met with headroom. Visuals confirmed: IDLE nested-shell orb + core/spine column + waves; SPEAKING bust with lit face; DORMANT diffuse cloud. Console: one first-load 404 (favicon), "THREE.Clock deprecated" (R3F internals), powerPreference notice.
Task 5: review (sonnet, aeacf24c4ec46ec5f) spec ❌ on one point, quality Needs fixes. Important 1 (plan-mandated): `aberration` is computed by `computeFrame` but no `SimUniforms` key exists. Ruling: no change in Task 5 — `aberration` is a post-pass parameter and Task 6 wires it (`ParticleSystem` writes a shared ref, `PostPass` calls `pipeline.setAberration`); the brief's "same keys" prose overstated the sim uniform set — cost if wrong: none (Task 6 is the next task and its brief already carries the wiring). Important 2: `packTargets` (new pure logic) shipped without a unit test → fix round 1: extract it to `sim/pack.ts` (no three imports) with a TDD test.
Task 5: minor (deferred): `packTargets` builds a 4·n·4 float array synchronously on every tier (re)build (~6.4 M floats at ultra).
Task 5: minor (deferred): the `shapeCircle()` typing cast is duplicated in `compute.ts` and `wavesSystem.ts` (cross-referenced comment); extract a helper if a third site appears.
Task 5: fix round 1/5 (1 addressed, 0 open — `packTargets` extracted to `sim/pack.ts` with 4 unit tests; commits d5c3fff..13b1fe6); re-review (haiku, ae8bcad0c976b9376) clean; trailers verified on 5c7fafc, d5c3fff, 13b1fe6.
Task 5: complete (commits 8a9737f..13b1fe6, review clean, 1 parked: WebGL2 residual → PC session branch `b5-05-webgl-fallback`). PR #7 squash-merged as 09c984b on main.

## Hand-off (2026-09-06)

Ali moved development to the gaming PC (Remote Control session "Kairos setup and installation"). This ledger is copied verbatim to `docs/plans/phase-b5-ledger.md` on main so the PC session can recreate `.superpowers/sdd/phase-b5/progress.md` and resume with superpowers:subagent-driven-development at **Task 6** (post-processing). Tasks 1–5 are `complete` above; briefs regenerate with `scripts/task-brief docs/plans/phase-b5.md N`. Open parked item: Task 5 WebGL2 residual (PC branch `b5-05-webgl-fallback`, may land before or after Task 6 — rebase whichever comes second). Deferred minors are listed per task above and feed the final whole-branch review after Task 9. Gate evidence so far: desktop 60 fps met on the RTX 5070 (Task 5 entry); phone 30 fps, Ali's visual approval (Task 7 playground) and perf CI (Task 9) remain.

## PC session (Tasks 5-residual, 6, 7)

Task 5 residual: FIXED on the PC (PR #8, squash-merged c726004). Root cause: under WebGL2 transform feedback a storage read at a computed cross-particle index (`instanceIndex + shapeId·n`) silently degrades to an attribute read at the particle's own index — every shape drew block 0 (HUMANOID), deterministic on the RTX 5070 (5/5 fresh runs), so the "intermittent" laptop report was the compile failure masking a constant wrong-block read. Ruling: kernel reads CPU-fed `targetA`/`targetB` vec4 slot buffers rewritten whole by `Sim.setShapes()` on morph-endpoint change (4 storage buffers ≤ the 4-varying floor); material reads region/spineT from a static `InstancedBufferAttribute` and depth via `positions.toAttribute()` — why: fixed-index reads are attribute-compatible on every backend — cost if wrong: one CPU `Float32Array.set` of n·4 floats per state transition. Kills the Task 5 "packTargets 4·n·4 sync alloc" minor (packTargets removed; `pack.ts` now carries `packShape`/`packRegionSpine` + tests). Not taken: three version bump (unnecessary once no computed cross-particle reads remain).

Task 6: complete (PR #9, squash-merged a3fef32). Ruling (1): bloom defaults retuned from the brief's {0.9/0.35/0.75} to full {0.35/0.08/3.5}, cheap {0.25/0.05/4} — why: the additive cloud sums far past 1.0 in the HDR buffer, sub-1 thresholds bloomed everything (frame washed white, tone mapping shifted overexposure pink); only the stacked core/spine sprites cross 3.5 — cost if wrong: numbers, and Ali retunes live in Task 7. Ruling (2): `chromaticAberration` gets an explicit `vec2(0.5, 0.5)` center — why: three 0.185.1's addon documents "null → screen center" but passes the null into the shader call and crashes the node builder — cost if wrong: none (identical semantics). Ruling (3): `computeFrame`'s aberration reaches PostPass through an owner-side setter callback into a shared ref — why: React Compiler lint forbids mutating a prop-passed ref — cost if wrong: none. Observation for Ali's Task 7 session: mid tier (60k) currently reads better than ultra (400k, saturates white) — ultra likely wants lower per-particle alpha/brightness.

Task 7: complete (this PR). CRITICAL DISCOVERY (cost a debugging session): with R3F 9.7 + three 0.185.1's async WebGPU init, ANY re-render of the `<Canvas>`'s parent component makes R3F run the async `gl` factory a second time against the same canvas — the sim's GPU node caches then straddle two renderers and the scene silently draws nothing (loop keeps running, clean console; a `setClearColor` probe still presents). The original playground layout subscribed the page component to `frames` (re-render every 30 frames) → black canvas on /dev/avatar while /bench/avatar (no React subscriptions) worked. Ruling: `AvatarCanvas`'s gl factory is a `useCallback`; the playground isolates the canvas in a `CanvasSlot` component whose only state is the tier override (its one re-render remounts the canvas wholesale via key); readout/stepper/energy/Leva live in sibling components — why: keeps every store-driven update away from the canvas parent — cost if wrong: layout only. Consider an upstream R3F issue + a lint/comment guard on AvatarCanvas consumers. Bloom sliders: threshold range 0–8, defaults from `bloomParams("full")` (brief's 0.9/0.75 predate the Task 6 retune). Deviation: tier select renders inside CanvasSlot (bottom-left) instead of the aside.

Task 8: complete (this PR). Ruling (1): the stage's canvas lives in a memoized `CanvasLayer` with stable callback props — why: the Task 7 rule (a re-render of the Canvas's parent during async WebGPU init kills the scene) and the stage re-renders on every streamed ribbon word — cost if wrong: layout only. Ruling (2): `demoOpts()` stretches `thinkMs` to 1.6 s when the turn starts from DORMANT/WAKING — why: the brief's claimed sequence was wrong: a FIRST_TOKEN arriving during the 1.2 s wake is dropped by the machine and the turn never reaches SPEAKING (caught by the new E2E, deterministic) — cost if wrong: 0.9 s of extra THINKING on cold turns. Verified on the PC: full loop DORMANT → click-wake (cue) → WAKING → LISTENING → THINKING → SPEAKING (ribbon streams, face pulses) → IDLE on `/` signed in; e2e 3/3.

## PC session (post-B5 polish)

Density fix: complete (PR #12, squash-merged 61dc80a). Ali reported ultra (400k) rendered as a white blob. Ruling: density-compensated per-particle brightness `clamp(sqrt(60000/n), 0.3, 1)` on main particles only — why: additive blending sums emitted light linearly with count, so total cloud light must stay ~constant relative to the mid tier; core/spine keep full brightness so the amber reads — cost if wrong: dimmer-than-ideal ultra, retunable in the playground.

Face readability: complete (PR #14). Ali reported the bust face "still seems empty". Two causes. Ruling (1): Lambert key light from sampled bust triangle normals (`sampleSurfaceWithNormals`, `Targets.humanoidNormals`, static instanced attribute) on main particles, faded in by a new `shade` frame value (HUMANOID weight of the current morph) — why: an additive cloud has no shading, so shallow relief (eye sockets, nose, lips) produces zero contrast; a key light restores it and the dark side doubles as ambient occlusion — cost if wrong: material-only, other shapes unaffected (shade=0). Ruling (2): the head core ball (60 % of core per docs/06 §2) dims to 15 % while humanoid, chest core to 80 % — why: it sits at eye level inside the skull and additive blending ignores occlusion, so its bloom floods the face; positions stay per spec — cost if wrong: dimmer head halo, retunable. Verified: 4 new TDD unit tests (78 total), headed-Chrome screenshots on the RTX 5070 show readable eye sockets/nose/lips in LISTENING at ultra. Note: the perf spec fails on THIS PC locally (headless chromium accumulates zero frame stats, identical on main — environment, not code; CI SwiftShader is the gate).

Polish T1 (avatar-polish.md): complete (this PR). Ali's three state complaints. Ruling (1): DORMANT nebula reworked from a curl-noise ball into a flattened 3-arm spiral "sleeping galaxy" + new per-state `sizeScale` in `StateParams` (DORMANT 0.55, OFFLINE 0.7) — why: Ali: "dormant shape is very poor and the particles are somehow large"; a designed shape reads at a glance where a noise ball reads as a blob — cost if wrong: shape/scale retunable, snapshot updated. Ruling (2): eyes — measured real anchors from bust.glb (probe script: sockets ±0.12/0.44/0.35, mouth 0/0.27/0.44, recorded in canonical.ts), socket darkening + pupil glow gated by `shade`, AND the head-core cluster moved from eye level to a new `coreHead` crown anchor — why: the core bloom sat exactly between the eyes and eclipsed them; no amount of socket contrast could win — cost if wrong: the "third-eye" look from the references is traded for readable eyes (Ali's explicit ask). Ruling (3): SPEAKING jaw is a TARGET displacement (region below the mouth drops with an open/close oscillation, 9 rad/s × speech energy), not a force — why: forces churn particles into fuzz, target displacement moves the chin as one coherent piece — cost if wrong: amplitude/rate are constants, easy retune. Supporting fixes: face pulse 0.8→0.3 (sustained speech energy dissolved the whole head), ear-pull radius 0.6→0.35 (covered the whole head, throbbed with the energy envelope), sparkle share 10 %→4 % and ×1.5→×0.7 (read as all-over fuzz under live treble). Verification gotcha: a HEADED Chrome window that is not foreground throttles rAF to ~1 fps — morphs crawl and every screenshot looks like fuzz; page.bringToFront() in the driver scripts before judging visuals.

Polish T2+T3 (avatar-polish.md): complete (one PR — both are small pure-visual additions). T2 ribbons: thin glowing contour bands wrap the bust (brightness modulation on main particles: sin of y·7 + z·1.5 drifting at 0.35/s, crest > 0.84, ×2.2 boost), gated by `shade` and masked within 0.3 of the eye anchors so the face keeps its contrast — why brightness-only: zero sim cost, WebGL2-safe, retunable — cost if wrong: constants. T3 halo: three static rings behind the head (`targets/halo.ts`, r 0.62/0.88/1.16 at z −0.62, 4.5k sprites) with an angular shimmer sweep, faded by `shade`, riding the waves tier gate (off on Low) — why a separate tiny system like waves: the morph slots stay untouched — cost if wrong: one extra draw call on mid+.

Polish T4 (avatar-polish.md): complete (this PR). Alive background: waves() reworked from two corner sheets into a full-width ridged-noise mountain range ((1-|n|)² crests, farther rows taller, near field keeps the centre open so the range never crosses the bust); wavesSystem gains energy veins — noise iso-line streaks that flare in slow pseudo-random bursts (sin-product flash envelope, pow 4). Why heightfield + brightness only: zero sim cost, same sprite count — cost if wrong: constants, and the waves snapshot is the only test artefact. Process note: CI runs `pnpm format:check` at the root — turbo lint does NOT cover prettier for apps/web; run format:check before pushing (PR #16 lost a CI round to this).

Polish T5 (avatar-polish.md): complete (this PR). Directional assembly: during WAKING each particle's morph progress is gated by its destination x plus jitter, compared against the LINEAR waking progress carried in a new `assemble` uniform — why linear not eased: WAKING uses easeOutExpo, which passes 0.85 within the first quarter of the wake, so eased-morph delays all fire at once and no sweep reads — cost if wrong: constants. Honest note: within the spec'd 1.2 s wake the sweep reads as directional streaking, not a slow theatrical "ASSEMBLING… 56 %"; that variant needs a longer WAKING (state-machine duration is part of the approved seven-state design + FIRST_TOKEN timing), so it goes to Ali's playground session as a decision, not a unilateral change.

Polish T6 v1 (avatar-polish.md): complete (this PR). Likeness from Ali's photo (corpus/raw/likeness/, one frontal shot). Ruling: procedural silhouette features instead of single-image 3D reconstruction — swept-up hair (scalp lift: top 0.055 + quiff 0.075, sides faded), rounded-rect glasses on the measured eye plane (superellipse rims + bridge + temples to the ear anchors), beard fuzz cloned along the jaw band; donors are low-torso CHEST particles reassigned in the HUMANOID target only, so everything dissolves with the bust — why: at particle resolution likeness is carried by silhouette; a real mesh reconstruction needs multiple photos AND a new local ML toolchain (heavy deps), which is Ali's call and stays open as T6 v2 — cost if wrong: all constants in targets/likeness.ts, photo never leaves the machine. Verified on the RTX 5070: glasses read unmistakably; hair volume and beard density visible.

## Wireframe bust (avatar look v2 — docs/plans/avatar-wireframe.md)

L1: complete (this PR). Ruling (1): contour loops come from CPU mesh slicing (`lines/slice.ts`, 120 planes over the full mesh height y ±0.9, resampled at 0.012) drawn with `LineSegments2` + `Line2NodeMaterial` (pixel width 1.6, additive, no depth) — why: Ali's reference is a line drawing; sprites can never read as continuous strokes (approach A in the spec) — cost if wrong: one extra draw call, ~30 k segments (spec budget of 15 k revised upward: the torso needs the full height; instanced quads make this negligible, 5 ms frames on WebGL2 mid tier). Ruling (2): open chains (mesh boundary at the bust base) are kept as polylines rather than dropped — why: the base rows would otherwise vanish — cost if wrong: none. Bug found on the way: the first cut stopped at y −0.55 and the "missing torso" was simply below the slice range (diagnosed with a per-plane loop census on the real GLB, not by guessing). Verified: `?webgl=1` boots the fat-line material at 5 ms p50 on mid (no fallback needed), WebGPU ultra screenshot: head-to-torso wireframe.

L2: complete (this PR). Orange core on the lines: colour = mix(lineCyan, coreLine→coreLineHot by coreHeat, coreGlow^0.6) where coreGlow falls off from a point just below the face anchor and only on camera-facing (z > 0) parts of the loops, breathing with corePulse; a slow brightness wave (sin over arc t, phase per slice) makes the wireframe flow — TSL only, no CPU updater yet (spec's CPU flow moved to L8 with the other deformations). Ruling (1): where the orange lives the lines get DIMMER (×(1 − 0.35·coreGlow)) and a soft radial `CoreFill` billboard behind the face supplies the warm glow — why: dense additive stacking of bright lines on the face front sums to white under bloom (threshold 3.5) and erases the hue; the reference's orange is a fill behind orange-tinted lines — cost if wrong: one sprite. Ruling (2): particle core lamps and spine sprites fade fully with `shade` (opacity, not colour — stacked sprites cross the bloom threshold even at 4 % colour) and dust dims to 15 % — why: the reference has no lamps; the vein tree (L4) takes the spine's place — cost if wrong: none for orb/galaxy states (shade = 0 there).

L3: complete (this PR). Likeness in lines. Ruling (1): the hairstyle lift moves the MESH vertices (`lines/likenessMesh.ts` liftMesh, area-weighted vertex normals, same lift rules as the dust version) before both slicing and particle sampling — why: one source of silhouette; lines and particles can never disagree — cost if wrong: none (pure function, tested). Ruling (2): glasses are real fat-line loops (superellipse rims, bridge, temples to the ear anchors) appended to the contour set with slice attr −1 and drawn brighter/whiter than the contour mesh — why: the dust glasses (2.5 % of particles) are redundant under lines and the loops read cleanly through the orange fill — cost if wrong: constants. `targets/likeness.ts` keeps GLASSES geometry as the single source and the hair-volume/beard dust; its scalp lift and glasses donors were removed (humanoid snapshot updated deliberately).

L4: complete (this PR). Vein spine + chest node: `lines/veins.ts` (pure, 3 tests — connectivity by construction, bounds, six-fold node symmetry, determinism) grows a lightning tree from the throat (0, 0.16, 0.30) to the chest node (0, −0.25, 0.30) with three branch generations and a six-spoke snowflake; `lines/VeinLines.ts` draws it as dashed fat lines (dash 0.07 / gap 0.018, width 2.3) whose dash offset advances with time (the pulse travels downward) plus a brightness wave over path distance, brightness ∝ corePulse + bass, warmer with coreHeat, fading with shade. Ruling (1): the grower blends every step's heading 35 % toward straight-down-with-a-pull-to-the-axis and steers side branches inward when the trunk has drifted — why: an unconstrained random walk sent branches off the torso into empty space (first screenshot) — cost if wrong: constants. Ruling (2): torso contour lines dim 55 % in a column between throat and node so the orange tree has contrast — why: the reference's torso centre is dark behind the veins — cost if wrong: one multiplier. Test lesson: branches are emitted depth-first (children before parent), so "segment 0 is the trunk" was a wrong assumption; the test now finds the trunk by generation.

L5: complete (this PR). Halo rings are now four dashed fat-line circles (`lines/halo.ts` geometry + `lines/HaloRings.ts`, radii 0.62/0.80/0.98/1.16, per-ring dash/gap picked in TSL from a ring attribute, dashes drifting in alternating directions via offsetNode × direction attribute, angular shimmer, outer rings fainter) replacing the T3 point rings (`sim/haloSystem.ts`, `targets/halo.ts` deleted). Spark plume (`lines/Sparks.ts` createSparks): 1.5 k sprites rising from a cone above the crown on a looping per-particle lifetime (fract(time/2.4 + seed)), noise sway, age fade, emission ∝ treble — no compute pass. Starfield (createStarfield): 5 k static sprites at z −3.5…−6 with hashed brightness and a slow twinkle. All fade with shade except the stars; all ride the waves tier gate (off on Low). Ruling: rings/sparks/stars are TSL-animated static buffers rather than sim particles — why: zero compute cost, deterministic, and they never need to morph — cost if wrong: none. Fix on the way: `Node<"float">` typing for a select-chain and `atan(y, x)` is a TSL function, not a method (the runtime error silently blocked scene init → screenshot timeout; typecheck caught it).

L6: complete (this PR). Mountain ridge veins: `targets/waves.ts` now exports `waveHeight(x, z)` (the ridged-noise heightfield, module-level noise so output is unchanged — waves snapshot untouched) and `lines/veins.ts` grows `ridgeVeins()` — 12 polylines whose heading blends 30 % toward the height gradient each step so they climb onto crests and ride them, y sitting 0.012 above the surface (test: every vertex within 0.03 of `waveHeight`). `lines/RidgeVeins.ts`: dashed fat lines with a flowing dash offset, an always-on ember plus per-vein slow lightning flares (product-of-sines envelope phased by vein index) with a travelling spark; background layer, not shade-gated. Ruling: a bust clearance column at every depth (`ridgeClearance(z)`: |x| ≥ 1.35 near, 1.15 mid, 0.9 far) — why: the bust is additive and never occludes, so far-field veins behind the neck showed straight through it (screenshot) — cost if wrong: a few fewer veins near the centre.

L7: complete (this PR). HUD chrome: `avatar/Hud.tsx` renders `STATUS: <STATE>` and, while WAKING, `ASSEMBLING… NN%` from a new store field `assemble` that the frame step publishes quantised to whole percents (so the store only updates when the displayed number changes); the component subscribes on its own, so per-frame updates re-render only the HUD, never the canvas's parent (Task 7 rule). System monospace stack, uppercase, 0.18 em tracking, 70 % cyan, tick marks; nothing blinks. Mounted top-right in `AvatarStage` beside the chat button; the stage section is now `overflow-hidden` (the off-screen chat drawer was scrolling the page sideways). E2E: the demo spec asserts `status: idle` after the turn and no ASSEMBLING line outside WAKING. Process lesson: local e2e runs `next start` on port 3100 and REUSES a running server — rebuild (`pnpm build`) and kill the stale server before trusting a local e2e on new UI; CI always builds fresh.

L8: complete (this PR). State behaviours on the lines. TSL: reveal sweep — each segment's opacity gates on the linear `assemble` progress with the same x-based delay mapping as the particle kernel, so wireframe and dust assemble together; `u.tint` now multiplies the line colour (OFFLINE's red). CPU: `LineBust.update()` rewrites the fat-line interleaved buffer from the immutable base positions for per-region index sets — jaw (shared `sim/jaw.ts` constants + pure `jawOffset`, unit-tested; the kernel now reads the same constants), listen ripple from the nearer ear, THINKING twist of the head around its axis (angle ∝ vortex · height · slow sine), OFFLINE fray (noise displacement during the freeze and the red dissolve). Ruling: a touched-vertex mask restores everything written last frame before applying this frame's effects — why: the first version tracked per-effect "active" flags and the THINKING twist survived into SPEAKING (screenshot-proven); the mask makes it impossible for an effect to outlive its state — cost if wrong: an extra pass over touched vertices only (idle frames cost nothing; THINKING ≈ 24 k vertices, still 5 ms frames on the RTX 5070). Verification: demo-page captures THINKING (twist) → SPEAKING (forward-facing, jaw) → IDLE.

L9 (self-pass before Ali's, this PR): the seven states captured at ultra and compared with the reference. Bug found: THINKING scrambled the head into stacked discs and the scramble grew with time. Root cause: three's `LineSegmentsGeometry.setPositions` keeps the Float32Array it is given (no copy), so the "immutable base" in L8 WAS the live GPU buffer — every restore was a no-op and every deformation accumulated frame after frame (the 5°/frame turn integrated into radians; the jaw and ripple drifted the same way, only slower). Fix: the CPU updater is now the pure module `lines/deform.ts` (`createLineDeformer(live)` snapshots its own base; 6 tests, including "the same inputs give the same vertices after 60 frames" and "restore leaves the buffer equal to the base"), and LineBust only forwards `needsUpdate`. Ruling (1): THINKING is a small whole-head turn (peak 0.09 rad, blended in over the neck y 0.2→0.4) instead of the per-height twist — why: the loops are see-through with no depth test, so turning shifts each loop's front and back halves in opposite directions and the deep ear-level loops shear into visible slivers long before a bigger turn reads as a turn (screen-space projection of the real slices: a 0.09 rad turn moves loop extents by ≤ 1 % of the frame); the per-height twist additionally slid adjacent loops apart — cost if wrong: one constant (`TURN_RAD`). Ruling (2): vein tree slightly larger for the reference's prominence — chest snowflake NODE_R 0.14 → 0.17, vein line width 2.3 → 2.8 — Ali's tuning pass may move these again. Verified: THINKING captures at three phases (clean turn, no discs), SPEAKING on the demo page (jaw moving, bust intact after 4 s), LISTENING unchanged, 5 ms p50 at ultra, 105 unit tests, typecheck/lint/prettier clean.


## Scene v3 — "Neural Bust" (Ali's SCENE_PLAN.md, 2026-09-06)

Plan `docs/plans/scene-plan.md`; per-phase log, deviations and open questions `docs/plans/scene-log.md`; branch `b5-30-scene` (one commit per phase, `scene: phase N — <name>` as the plan prescribes).

- Ruling: built in a separate git worktree (`C:\Users\USER\Dev\Kairos-scene`, outside OneDrive) — why: a second session (L9 self-pass, PR #28) was editing the main checkout at the same time; the pre-flight "no worktree" ruling assumed one session — cost if wrong: one extra `pnpm install --offline` (4.5 s) and a worktree to remove when the branch merges.
- Ruling: the scene is a standalone tree (`apps/web/src/scene/`, `/bench/scene`) and does not touch `AvatarCanvas`/the lines layer — why: the plan is a static LISTENING look with its own component tree and config; wiring it to the seven Avatar States is a separate decision for Ali — cost if wrong: the owner home still shows look v2 until that decision.
- Ruling: adaptation to three/webgpu + TSL (no drei / postprocessing / simplex-noise added; GLSL ported to TSL; RenderPipeline bloom + vignette; gradient drawn in-scene; tone mapping OFF via `flat` because the plan sets `toneMapped:false` on every visible material and three/webgpu tone-maps per frame, ignoring that flag) — why: CLAUDE.md §4 stack; the plan allows "adapt the API but keep the phases, values and acceptance criteria" — cost if wrong: the values that carry adapted units are commented in `sceneConfig.ts`.
- Ruling: bust source = the repo's `bust.glb` (the plan's preferred GLB path) with the jagged arm-crop boundaries straightened onto one slanted line per side and the bottom crop pulled below the frame; the plan's primitive fallback is implemented and switchable (`bust.source`) — why: side-by-side the primitives read as a pawn on a saucer — cost if wrong: the mesh carries nose/lips/ears the plan did not want (flagged; `bust.source = "primitives"` removes them).
- Ruling: values fitted to the mesh instead of the sphere head: `core.center` y 1.5 → 1.3, `neck.jawY` 1.02 → 0.95, `neck.nodeY` 0.42 → 0.15, `neck.controlY` 0.75 → 0.59 — why: the plan's numbers position the glow on the forehead and the sternum node mid-neck on a real head — cost if wrong: three numbers, one `?set=` away.
- Ruling: Phase 8 tuning `contours.frequency` 90 → 45, `post.bloomStrength` 0.43 → 0.25 (plan intensity 1.3 ÷ three's 3.0 mip-weight total → 0.43), `post.bloomThreshold` 0.55 → 0.3 — why: at 90 the 0.7 px lines bloomed into a solid cyan haze and on three's linear-luminance threshold gold (0.54), rings (≤ 0.25) and the blue landscape (0.13) never glowed, failing the phase's own acceptance; the plan values are kept in config comments and in `docs/screens/phase-8-alt-plan-values.png` — cost if wrong: three numbers.
- Pre-implementation adversarial design review (30 agents) confirmed 26 findings; the ones that changed the build: MSAA needed for fat lines and 2 px rings (`antialias: true` — the avatar factory uses false), FaceCore z must sit in front of the mesh nose (0.72), `screenUV.y` is top-origin (gradient uses the quad's own uv), vignette must multiply colour only (a premultiplied canvas fades corners to the page colour), transparent `Line2NodeMaterial` costs a per-frame framebuffer copy (neck strands opaque, opacity folded into colour), plan point sizes are PointsMaterial units (× tan(fov/2) for sprites), BloomNode strength/threshold semantics differ from pmndrs (documented in config), R3F overwrites `renderer.toneMapping` after the gl factory (hence `flat`), the gl factory must not write to the store before returning (deferred) and is idempotent per canvas.
- Process: `?set=path:value,...` on `/bench/scene` mutates `sceneConfig` for one page load — the plan's per-property feedback loop without code edits; prettier `--write` after every phase (CI runs root `format:check`); headed Chrome + `bringToFront()` for every screenshot (headless accumulates no frames on this PC).
- Correctness follow-up (2026-09-07, lens re-run after a usage-limit failure): `?set=` overrides applied in the browser only — the bench page is also server-rendered and a server-side mutation of the config singleton leaked one request's overrides into the next; `ready` gated on the bust mesh being in the scene (+ `error` on a failed load); the WebGPU renderer disposed on unmount via a tick-deferred cleanup that an immediate remount cancels (React StrictMode dev double-mount) — R3F's unmount only knows the WebGL renderer API; the `@/scene` barrel no longer re-exports the client-only canvas; e2e fails on page errors and waits for 30 scene frames on CI only (local headless Chromium accumulates no frame stats on this PC).
- CI backend (2026-09-07): the "no page errors" assertion turned the CI scene smoke red; three throwaway bisect runs (34098121956 / 34136858023 / 34137480858) showed the runner's SwiftShader WebGPU device lost with reason "destroyed" 40–140 ms after creation — no JS `destroy()` (traced via an init script), every layer set incl. a lone background quad, with/without MSAA, headless shell or new headless, avatar bench too — so no WebGPU frame has ever rendered on CI; WebGL2 renders there (ready 4.5–7 s, ~10 fps). Ruling: the scene smoke runs `?webgl=1` on CI; MSAA is `sceneConfig.render.antialias`; an externally lost device is reported as `error` — why: a test that asserts on a dead device is hollow — cost if wrong: WebGPU stays hand-verified on hardware (it is anyway: local headless Chromium has no adapter). Flagged for Ali, not applied: the avatar CI e2e + perf baseline (backend "webgpu") measured the same dead device.
