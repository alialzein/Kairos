# STATUS
Current phase: A1 / B5 — B5 implementation COMPLETE (Tasks 1–9 of `docs/plans/phase-b5.md` merged on `main`: PRs #2 #4 #5 #6 #7 + WebGL2 fix #8 + post #9 + playground #10 + hero #11 + perf CI this PR); gate items needing Ali listed under Blockers. Track A (A1) not started. Development runs on the gaming PC (since 2026-09-05).
Last session: 2026-09-07 evening (gaming PC) — PR #29 (scene v3 Neural Bust, rounds 1–3) MERGED on Ali's "merge" (squash 4ba7cae, worktree + branch removed, dev server on :3200 from the main checkout). Then Ali's PHASE 10 (particle pass) on branch `b5-31-particles`, one commit + screenshot per item in `docs/screens/phase-10/` (log: `docs/plans/scene-log.md` "Phase 10"): 10.1 landscape as a plexus network (jittered heightfield nodes, k-nearest edges ≤ 0.35, gold nodes by height², sprinkle), 10.2 bust particle shell (25k sampled sprites, fresnel alpha, new `shell` layer), 10.3 beaded contour lines (`contours.beads`, on/off screenshots), 10.4 beaded rings + drifting dust + beaded neck strands with a bright sternum core. Shared Phase 10 sprite (`softDisc`, per-point sizes, never tone mapped) and `particles.sizeScale` 3 (Ali's starting sizes were ~2 px). Ali's verdict: structure right, counts ~10× too low → PHASE 11 (density pass) on the same PR, one commit + screenshot per item in `docs/screens/phase-11/`: sizeScale 3 → 1.5; 11.1 landscape 160×40 per side (12,880 nodes, edges ≤ 0.12, rise to head height at the frame edges, fade only −0.75..−0.45, 4,000 dust per side; baseY −1.2 → −0.7 so the slope fills from the frame bottom); 11.2 shell 80k at alpha 0.03 + 0.6·fresnel, fill #020C22, lineWidth 0.04; 11.3 branching neck (2–3 sub-branches per strand, 120 beads, bright beads at forks/tips); 11.4 new `dust` layer (2,500 ambient + 1,500 crown plume on TSL time). Budget 108,987 sprites; p50 0.6 ms vsync-off on the RTX 5070. Awaiting Ali's approval of the 11.4 screenshot, then merge #30. Previous: 2026-09-06 (gaming PC, third round, session kairos-fa in a separate worktree) — AVATAR LOOK v3 "Neural Bust" from Ali's `SCENE_PLAN.md` built end to end, phases 0–8 (branch `b5-30-scene`, one commit per phase, plan `docs/plans/scene-plan.md`, log with every deviation `docs/plans/scene-log.md`, rulings in the ledger's "Scene v3" section): standalone `apps/web/src/scene/` tree + public `/bench/scene` (`phase`/`only`/`off`/`webgl`/`set` query params), TSL contour-line shader on the repo's bust mesh, face glow, gold neck circuitry, 9 rings, wireframe mountains, RenderPipeline bloom + vignette, HUD; 27 unit tests, e2e 5/5 on a production build, all repo gates green; screenshots `docs/screens/00-baseline.png` … `phase-8.png`. Phase 9 (motion/perf) waits for Ali's Phase 8 approval; the scene is not wired to the seven states (look v2 still runs the owner home). Second round — two Ali-reported visual defects fixed: ultra-tier white-blob (density-compensated brightness, PR #12) and the empty face (normal-based key light + head-core dimming, PR #14 — eye sockets/nose/lips now read; rulings in the ledger's "PC session (post-B5 polish)"). Earlier same day — Tasks 6–9 shipped: post pipeline (bloom tuned to core/spine on the additive HDR cloud, chromatic aberration on WAKING, vignette; two upstream three 0.185.1 bugs worked around), `/dev/avatar` playground (Leva, state stepper, tier override, synth/file/mic energy) with the critical R3F discovery (a re-render of the Canvas's parent during async WebGPU init silently kills the scene — canvas isolation rule in the ledger), owner home = AvatarStage (status ring, ribbon, chat drawer, demo turns, click-to-wake cue; the brief's demo timing dropped FIRST_TOKEN mid-wake — fixed and E2E-covered), perf CI job + baseline. Rulings in `docs/plans/phase-b5-ledger.md` (PC session sections). 2026-09-07 follow-up (same session): PR #29's CI went red on the new "no page errors" e2e assertion; bisected on a throwaway branch (runs 34098121956 / 34136858023 / 34137480858): the runner's SwiftShader WebGPU device is lost (reason "destroyed", no JS `destroy()` call — traced) 40–140 ms after creation on every WebGPU page, scene and avatar alike, any layer set, with or without MSAA, headless shell or new headless — CI has never rendered a WebGPU frame; WebGL2 renders there. Fix on the branch: the scene smoke runs `?webgl=1` on CI, MSAA became `sceneConfig.render.antialias`, an externally lost device now surfaces as the scene's `error`. Playwright runs one worker on CI (two SwiftShader browsers halved the smoke's rate and pushed the perf spec's p95 16 % over its baseline). Evening: Ali's feedback round 1 applied exactly, one commit + screenshot per phase (`docs/screens/feedback-1/`): eyes closed in geometry (the GLB bakes an eye slit + eyeball into its single mesh — no node to hide; `flattenCavity` fits the skin and parks the eyeball behind it), lineWidth 0.05 / rim 0.9 / exact #35C8FF (line boost 0), strands from the measured chin (jawY 0.67), rings 0.4 → 0.03, landscape amplitude 1.0 / broader / dropout 0.55 / visible points, layer checkbox bar removed, bloom 0.533 / 0.4, camera z 6.0. Then Phase 9 (motion + performance) shipped: camera drift, gold shimmer, ring breathing, neck pulse, reduced-motion, dpr cap, mobile columns. Round 2 (same evening): fixed 54° horizontal FOV with the vertical fov derived per aspect (framing aspect-independent; screenshots at 16:9 and half width), ridges to neck height with gold scattered by height², strands bowing outward. Decisions: scene stays on the bench until #29 merges; the seven-state wiring, the CI WebGL2 re-baseline and the /dev/avatar removal are separate PRs afterwards; look v2 is replaced; phone gate later by Ali; B5/Track A wait. Process: wait for approval before each next phase. Round 3: Phase 7 rebuilt as a real heightfield (36×14 grid, noise over world x/z, slope toward the viewer, bottom fade), skirt −3.0, core radius 0.42 — awaiting Ali's approval of the screenshot, then merge #29.
Next: Ali's approval of the Phase 11.4 screenshot (`docs/screens/phase-11/11-4.png`, live on `/bench/scene` :3200), then merge PR #30 (dev server stays up). Then, as separate PRs one at a time, each on Ali's go: (1) the seven avatar states on the scene, (2) CI avatar e2e/perf on WebGL2 with a re-recorded baseline, (3) remove /dev/avatar and look v2. Ali's approval of round 2 on `/bench/scene` (dev server on :3200), then merge PR #29. After the merge, in this order and each as its own PR: the seven avatar states on the scene (then remove /dev/avatar and look v2), the CI avatar e2e/perf on WebGL2 with a re-recorded baseline. Ali's review of scene v3 against the reference on `/bench/scene` (open questions in `docs/plans/scene-log.md`: mesh with nose/ears vs the plan's smooth primitives, line density 45 vs 90, bloom 0.25/0.3 vs the plan's 0.43/0.55, gold-ridge rule, mounting on the owner home = giving the scene the seven states); per-property feedback is applied in `sceneConfig.ts` (preview any value with `?set=`). Phase 9 is done; next is Ali's round-2 feedback (open: the faint closed-lid trace at the eye corners; the strands' curvature at the plan's control point is slight). Still open from before — AVATAR LOOK v2 ("wireframe bust", spec docs/superpowers/specs/2026-09-06-wireframe-bust-design.md, plan docs/plans/avatar-wireframe.md) CODE COMPLETE 2026-09-06 — L1–L8 merged (PRs #20–#27): CPU mesh slicer + fat-line contour bust, orange core striations + core fill, likeness (hair lift on the mesh; glasses built then REMOVED at Ali's request), lightning-tree vein spine + chest snowflake, dashed halo rings + spark plume + starfield, ridge lightning on the mountains, HUD chrome (STATUS / ASSEMBLING NN%), state behaviours on the lines (reveal sweep, jaw, listen ripple, thinking twist, offline fray). L9 self-pass done (PR #28: the L8 base/live buffer aliasing bug — deformations accumulated every frame, THINKING scrambled into discs — fixed via the pure `lines/deform.ts` + 6 tests; THINKING is now a small whole-head turn; vein tree slightly larger). Remaining: L9 = Ali's approval pass in /dev/avatar with the reference side by side (tune constants: core fill intensity, vein tree size, ring brightness; decide on a longer WAKING for a slower ASSEMBLING; likeness v2 needs side photos + ML-dep approval). Then the B5 phone gate, then Track A (A1 ingestion — "teaching Kairos"). DECISION FOR ALI (from the CI finding): the avatar's CI e2e and perf baseline (`tests/perf/baseline.ci.json`, backend "webgpu", p95 18.11 ms) measured an rAF loop on a dead device; proposal = run `/bench/avatar` e2e + perf on `?webgl=1` in CI and re-record the baseline (workflow_dispatch `update_baseline`) — changes what the B5.8 gate measures, so not applied.
Blockers: B5 gate — 30 fps on Ali's phone (`/bench/avatar?tier=mid` over the LAN; needs the firewall rules from an admin shell) and Ali's visual approval of the seven states in `/dev/avatar` — both need Ali. Desktop 60 fps already ✅ on the PC. CI's WebGPU is software-only and dies at start (see Last session), so every WebGPU gate is verified on real hardware by hand (headed Chrome on the PC; headless Chromium on the PC has no WebGPU adapter at all).
Gate history: A0/B0 ✅ 2026-09-03 (automated items) — CI green https://github.com/alialzein/Kairos/actions/runs/33782973057 ; privacy proof https://github.com/alialzein/Kairos/actions/runs/33760172580 ; auth tests brain 24 / web 13; "Vercel preview deploys" moved to Phase C by ADR-0014. Home stage ✅ 2026-09-06 (gaming PC, all runbook acceptance items): `docker compose ps` all six healthy (brain/memory/voice/redis/falkordb/caddy) + `/brain/health` `/memory/health` `/voice/health` ok via caddy; `pnpm supabase test db` 9/9 pgTAP pass; `ollama ps` qwen3.5:9b 5.6 GB **100% GPU** (RTX 5070, driver 616.56), test prompt answered in Arabic+English; first owner sign-in done (magic link via Mailpit → owner home renders), owner promoted in `profiles`, `OWNER_USER_IDS` set in both env files, `ALLOW_SIGNUP` back to false — non-owner session verifiably denied before promotion. Env gotcha: inline `#` comments in `.env` leak into values (broke brain's JWKS URL); comments now on their own lines. B5 ⏳ — desktop 60 fps ✅ 2026-09-06 on the gaming PC (RTX 5070, Chrome, WebGPU): `/bench/avatar?tier=ultra` p50 5.0 ms / p95 5.1 ms, `tier=high` 5.0 / 5.1, SPEAKING 5.0 / 5.1, DORMANT 5.0 / 5.2, IDLE/SPEAKING/DORMANT visuals confirmed; laptop (Intel Arc, WebGPU) `tier=high` p50 ≈ 16.7 ms / p95 ≈ 18–24 ms; unit tests web 71 / config 8 / shared 6 / scripts 16; e2e smoke 2/2 (also green on the PC). Pending: 30 fps on Ali's phone (`?tier=mid`), Ali's approval of the seven states (Task 7), reduced-motion (automated via tier tests + Low tier stop), perf CI baseline (Task 9).
